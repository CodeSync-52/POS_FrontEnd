export interface ICategoryData {
  group: string;
  id: string;
  category: string;
  status: string;
  action?: any;
}
export interface ICategoryDetailsData {
  id: string;
  label: string;
  fullName: string;
  action: string;
}
